/**
 * @deepseek-ai/dsh-client-ui-hub — invariant
 *
 * No runtime invariant: the hub settings UI is a pure presentation plugin that
 * registers into the settings.section slot. Its only runtime relationship is
 * the slot it registers into, and the slot system's own invariant covers that
 * contract.
 */
